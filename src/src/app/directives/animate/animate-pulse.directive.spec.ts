import { AnimatePulseDirective } from './animate-pulse.directive';

describe('AnimatePulseDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatePulseDirective(null);
    expect(directive).toBeTruthy();
  });
});
